## Problem (print screen to sharehold)

# All the part
1: Standardize the formatting of Chinese and English.
2: New sangha add as new member(separate from case), only the number who need service, he will get case
3: roles: view manager/full manager/team lead(see all case)/Socail worker(see their own case)/admin(edit member's profile and generate report)/volunteer(upload the case note[report]/document/view partial profile)
4: SK can not see the clinet module, only manager can view
5: To delete require two managers(the delete action need two manager, first manager to propose, second manager to approve)(delete report the same)
6: Onle manager can assign socail worker to case
7: any Socail work can propose to close the case, but only manager can approve8, only manager can generate report
8: audit logs only view/full manager can view
9: Senstive: Ordination Certicate, Medical records, finanical records, legal docs(only manager can view all and assign sharing permission)
10: Socail work can view only the documents of the service they support
11: all the docs are organized into four categories. on delete authroity
12: SOP for service request and changes(Socail worK can request, manager can approve)

# Dashboard
1: 4个cards，use the color, some element(small word) needn't mark
2: Make sure the correct color meaning

# Client
1: client needn't id num
2: Save the name, phone number only.
3: Client dercribe change member

# Case
1: num, 主要社工 needn't
2: Aduit part needn't
3: add a new calendar list to show the info of events(share client: all the calendar event follow the same format: running service num + service name: abbrev@venue)(Grape: followship，Mango: casework，[D81B60]cherry blossom: metta care)
4: Butto needn't in case overview, return button only need arrows
5: Each event must has a case note, if no, send email of elert

# Report
1：adjust the layout of the report dashboard, ID neeedn't, use update time
2: the report detail too much color and complex 

# User
1: This module name change to the manager
2: add user change to register user
3: Move to the backend

# Login
1: write a report for note verification
2: login and 2FA need has difference
